openapi: 3.0.1
info:
  title: internal server error test
  version: '1.0'
servers:
  - url: 'http://localhost:8000/'
paths:
  /fault:
    get:
      responses:
        "500":
          description: Everything's gone wrong!
