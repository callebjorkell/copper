openapi: 3.1.0
info:
  title: invalid schema test
  version: '1.0'
paths:
  /thing/{id}:
    parameters:
      - borken
    delete:
      responses:
        '204':
          description: Delete the thing!
