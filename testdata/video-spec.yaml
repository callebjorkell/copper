openapi: 3.0.1
info:
  title: minimal test
  version: '1.0'
servers:
  - url: 'http://localhost:8000/'
paths:
  /video:
    get:
      responses:
        "200":
          content:
            "video/mp4":
              schema:
                type: string
                format: binary
          description: A video
