openapi: 3.1.0
info:
  title: number test
  version: '1.0'
servers:
  - url: 'http://localhost:8000/'
paths:
  /mini:
    get:
      responses:
        "200":
          content:
            "application/json":
              schema:
                type: object
                properties:
                  number:
                    type: integer
                required:
                  - number
          description: A number!
